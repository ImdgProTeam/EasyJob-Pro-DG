<local:AnimatedDialogWindow x:Class="EasyJob_ProDG.UI.View.DialogWindows.UpdateConditionSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyJob_ProDG.UI.View.DialogWindows" 
        xmlns:usercontrols="clr-namespace:EasyJob_ProDG.UI.View.DialogWindows.Summaries.UserControls"
        mc:Ignorable="d"
        Height="800" Width="400"
        Title="{Binding Title}"
        Style="{StaticResource SummaryWindowStyle}"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <local:AnimatedDialogWindow.Resources>
    </local:AnimatedDialogWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Capture -->
        <TextBlock Text="{Binding Title}" Style="{StaticResource WindowTitleTextBlock}"
                   HorizontalAlignment="Left"/>

        <StackPanel Orientation="Vertical" Grid.Row="1"
                    Background="{StaticResource Brush.Background.Main}"
                    >
            <StackPanel.Resources>
                <Style TargetType="{x:Type usercontrols:UpdateReportBlock}">
                    <Setter Property="Margin" Value="0 5"/>
                </Style>
            </StackPanel.Resources>
            <TextBlock Text="   Below figures are result of comparison with previous loaded condition."
                       TextAlignment="Justify" 
                       TextWrapping="Wrap"
                       FontStyle="Italic"
                       Margin="10 0"/>

            <usercontrols:UpdateConditionSummaryControl/>

            <CheckBox Content="Show this window on Condition Update"
                          IsChecked="{Binding ShowSummaryOnUpdateCondition, Mode=TwoWay}"
                          Margin="10 5"/>
        </StackPanel>

        <!-- Bottom -->
        <Grid Grid.Row="2">
            <StackPanel>

                <Button Content="Close" 
                        Command="{Binding CloseCommand}" 
                        IsCancel="True" 
                        Style="{StaticResource SummaryWindowButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</local:AnimatedDialogWindow>
